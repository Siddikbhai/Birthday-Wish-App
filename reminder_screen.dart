import 'package:flutter/material.dart';

class ReminderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Set Birthday Reminder')),
      body: Center(
        child: Text('Coming Soon: Reminder Feature'),
      ),
    );
  }
}
